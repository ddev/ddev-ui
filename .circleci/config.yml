version: 2
stages:
  build:
    machine: true

#    environment:
#      GOPATH: /home/circleci/go
    working_directory: ~/ddev-ui

    steps:

      - run:
          name: Update npm
          command: |
            curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
            sudo apt-get -qq install -y build-essential nodejs npm wine wine-mono

      - checkout

      - run: make linux

